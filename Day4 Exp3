def constructLowerArray(arr, countSmaller, n):
 
 
    for i in range(n):
        countSmaller[i] = 0
 
    for i in range(n):
        for j in range(i + 1, n):
            if (arr[j] < arr[i]):
                countSmaller[i] += 1
 
